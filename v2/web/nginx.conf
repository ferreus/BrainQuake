server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback -- react-router-dom routes are client-side only.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
